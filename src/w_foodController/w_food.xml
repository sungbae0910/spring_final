<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="food">
 <select id='foodcode' parameterType="String" resultType="java.util.List">
 	select foodcode
 	from w_food
 	where foodtype = #{type}
 </select>
 
 <select id='w_select' parameterType="java.util.List" resultType="w_foodController.FoodVo">
  select f.foodcode , f.foodname, f.foodprice, ,fi.foodcode , fi.foodorifile, fi.foodsysfile
  from w_food f , w_food_image fi 
  where f.foodcode = fi.foodcode
  and <foreach collection="list" item="i" index="index" separator="or">f.foodcode = ${i.foodcode}</foreach>

 </select>
 
 
</mapper>
