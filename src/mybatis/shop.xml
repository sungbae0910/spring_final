<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shop">
	<select id="header_category_menu" resultType="shopController.ShopItemVo">
		select category_name
		from shop_category
	</select>
	<select id="main_earring_select" resultType="shopController.ShopItemVo">
		select I.item_name, I.item_price, I.item_main_photo
		from shop_item I join shop_category C
		on I.item_category = C.item_category
		where I.item_category = 0
	</select>
	<select id="main_necklace_select" resultType="shopController.ShopItemVo">
		select I.item_name, I.item_price, I.item_main_photo
		from shop_item I join shop_category C
		on I.item_category = C.item_category
		where I.item_category = 1
	</select>
	<select id="main_ring_select" resultType="shopController.ShopItemVo">
		select I.item_name, I.item_price, I.item_main_photo
		from shop_item I join shop_category C
		on I.item_category = C.item_category
		where I.item_category = 2
	</select>
	<select id="main_bracelet_select" resultType="shopController.ShopItemVo">
		select I.item_name, I.item_price, I.item_main_photo
		from shop_item I join shop_category C
		on I.item_category = C.item_category
		where I.item_category = 3
	</select>
	<select id="item_more_select" parameterType="Integer" resultType="shopController.ShopItemVo">
		select I.item_name, I.item_price, I.item_main_photo, C.category_name
		from shop_item I join shop_category C
		on I.item_category = C.item_category
		where I.item_category = ${_parameter}
	</select>
</mapper>