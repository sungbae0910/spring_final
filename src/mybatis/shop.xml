<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shop">
	<select id="item_select" resultType="shopController.ShopItemVo">
		select item_name, item_price, G.category_name
		from shop_item I join shop_category G
		on I.item_category = G.item_category

	</select>
</mapper>